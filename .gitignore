# Credentials — never commit
.env

# Personal quick-reference with real email addresses — kept local only
# (the repo is public, so we don't publish personal emails).
CHEATSHEET.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
